import React from 'react';
import Camera from '../components/Camera';

const CameraPages = () => {
    return (
        <div>
            <Camera></Camera>
        </div>
    );
};

export default CameraPages;